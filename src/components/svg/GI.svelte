<div />

<style>
	div {
		display: flex;
		justify-content: center;
		width: var(--size);
		height: var(--size);
		background: var(--color);
		mask-image: url(genshin.svg);
		mask-mode: luminance;
	}
</style>
